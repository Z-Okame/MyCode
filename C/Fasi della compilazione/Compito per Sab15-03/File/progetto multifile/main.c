#include <stdio.h> //per printf
#include "functions.h"  // Includo il file header

int main() {
    int a = 10, b = 20;
    printf("Somma: %d\n", somma(a, b));
    printf("Moltiplicazione: %d\n", moltiplicazione(a, b));
    return 0;
}
